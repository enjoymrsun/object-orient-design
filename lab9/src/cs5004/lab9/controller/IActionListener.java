package cs5004.lab9.controller;

/**
 * Interface as the action listener for the Swing GUI software.
 */
public interface IActionListener {
  /**
   * Action needs to perform according to a specific string representation.
   *
   * @param action type of action you want to perform
   */
  void action(String action);
}
